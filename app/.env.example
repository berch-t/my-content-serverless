# Configuration API Locale et Cloud Function
# Copiez ce fichier vers .env.local et ajustez les valeurs

# Mode API par défaut ('local', 'cloud', ou 'auto')
# - local: utilise uniquement l'API locale
# - cloud: utilise uniquement la Cloud Function
# - auto: essaie local d'abord, fallback vers cloud
NEXT_PUBLIC_API_MODE=auto

# URL de l'API locale (FastAPI)
NEXT_PUBLIC_LOCAL_API_URL=http://127.0.0.1:8000

# URL de la Cloud Function GCP 
NEXT_PUBLIC_CLOUD_FUNCTION_URL=votre-url-serverless-function

# Configuration Next.js
NEXT_PUBLIC_APP_ENV=development
NEXT_PUBLIC_APP_VERSION=1.0.0